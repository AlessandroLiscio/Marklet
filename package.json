{
  "name": "marklet",
  "version": "0.1.0",
  "description": "Lightweight, open-source Markdown viewer. Reading first.",
  "license": "MIT",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "check": "svelte-check --tsconfig ./tsconfig.json",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:e2e": "wdio run tests/e2e/wdio.conf.ts",
    "tauri": "tauri",
    "contrast": "node scripts/check-contrast.mjs",
    "size": "node scripts/size-report.mjs"
  },
  "devDependencies": {
    "@sveltejs/vite-plugin-svelte": "^7.3.0",
    "@tauri-apps/cli": "^2.11.4",
    "svelte": "^5.57.0",
    "svelte-check": "^4.7.6",
    "typescript": "^5.9.3",
    "vite": "^8.2.2",
    "vitest": "^5.0.0"
  },
  "dependencies": {
    "@tauri-apps/api": "^2.11.1"
  },
  "//": "Lazy-loaded rendering deps (mermaid, katex, highlight.js) and editor deps (@codemirror/*) are added in phases P4 and P7 respectively. They must never be imported at module top level in src/main.ts. See .claude/skills/size-budget/SKILL.md."
}
