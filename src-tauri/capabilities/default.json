{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Minimal surface. The frontend gets NO filesystem, shell, or http permission. Every privileged action goes through a named, validated #[tauri::command] in src/ipc.rs, which is the only place path validation lives.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "core:window:allow-show",
    "core:window:allow-set-title",
    "core:window:allow-start-dragging",
    "core:event:allow-listen",
    "core:event:allow-unlisten"
  ]
}
